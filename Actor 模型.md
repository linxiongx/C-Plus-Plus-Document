# Actor 模型

## 核心思想

多线程的根本问题是：**两个人同时改一份数据**。

解决思路有两种：

- **加锁**：同一时间只允许一个人进入，其他人等
- **Actor**：这份数据只有一个人负责，其他人想改，给他发消息，他来改

Actor 的本质是把"并发访问"变成"串行处理"，不是靠锁，而是靠**数据归属权**。

------

## 对应到代码

`ImageIn` 归 `Stq` 这条串行队列"所有"，任何人想碰它，都必须提交任务进队列：

```cpp
// ❌ 错误：主线程直接读，与工作线程产生竞争
QmImage img = ImageIn;

// ✅ 正确：发消息给队列，由队列内部来读
Stq->submit([&](QmImage* out) {
    *out = ImageIn;   // 此时没有其他任务在跑，安全
}, &img).wait();
```

规则只有一条：**谁的数据，谁的队列，外人免进。**

------

## 对比加锁

|          | 加锁                                 | Actor                                            |
| -------- | ------------------------------------ | ------------------------------------------------ |
| 互斥方式 | 手动 lock/unlock                     | 队列串行自动保证                                 |
| 死锁风险 | 有                                   | 无                                               |
| 代码侵入 | 每处访问都要加锁                     | 只需保证数据不外漏                               |
| 性能     | 锁竞争激烈时线程互相阻塞，吞吐量下降 | 无锁竞争，但串行队列本身是瓶颈，高并发下任务堆积 |

两者没有绝对的性能优劣，取决于场景：

- **读多写少**：加锁可以用读写锁（`shared_mutex`）允许并发读，Actor 串行处理反而更慢
- **写多**：锁竞争激烈，线程大量阻塞，Actor 的串行队列优势明显
- **任务耗时长**：Actor 队列堆积严重，不如加锁后并行处理

------

## 局限

数据归一个队列串行处理，意味着**无法并行**。如果任务之间本来就互不干扰，用 Actor 反而是浪费。

------

## 一句话

> 不要共享数据，要传递消息；不要抢锁，要排队。