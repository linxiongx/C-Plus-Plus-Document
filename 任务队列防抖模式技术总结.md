# 任务队列防抖模式技术总结

## 一、问题背景

在 GUI 图像处理场景中，用户操作（如拖动滑块）会在极短时间内**连续触发**图像调整事件。若每次触发都提交一次完整的图像处理任务，会造成：

- 大量冗余计算（中间帧无意义）
- 任务队列积压
- UI 卡顿、响应延迟

------

## 二、核心机制解析

cpp

```cpp
std::atomic<long long> BcgSync;
void QmImageViewModel::on_BCGConverter_AdjustImage(std::function<void(UserImage, CRgn*)> func)
{
    BcgSync++;                          // ① 主线程：立即计数+1
    Stq->submit([&, func]() -> void {   // ② 提交任务到队列（异步）
        if (--BcgSync > 0)              // ③ 工作线程：计数-1，检查是否有更新请求
            return;                     // ④ 有更新请求 → 放弃，让更新的任务处理
        // 真正的处理逻辑
        if (DlgForAlg.first.IsEmpty() || !DlgForAlg.second->isShow())
            ImageAfterAlg = ImageIn;
        func(ImageAfterAlg, getROI());  // ⑤ 只有最后一个任务才执行到这里
    });
}
```

**执行时序图：**
```
主线程调用:    ①call  ②call  ③call
BcgSync值:     1      2      3
               ↓      ↓      ↓
任务队列:    [task1][task2][task3]
               ↓      ↓      ↓
工作线程执行:  2>0    1>0    0==0
               ↓      ↓      ↓
结果:         SKIP   SKIP  EXECUTE ✓
```

---

## 三、设计模式分类

| 维度 | 说明 |
|------|------|
| **模式名称** | 任务合并 / 末尾触发防抖（Trailing Debounce） |
| **触发条件** | 只执行队列中**最后一个**任务 |
| **实现方式** | 计数器（而非 Timer） |
| **适用场景** | 任务队列顺序执行、任务提交频率远高于执行速度 |

与常见防抖方案的对比：

| 方案 | 原理 | 依赖 | 适用 |
|------|------|------|------|
| **BcgSync 计数器** | 提交时 ++，执行时 -- 检查 | 串行队列 | 本例 ✓ |
| Timer Debounce | 重置定时器，延迟执行 | 定时器 | UI 事件 |
| Throttle | 固定时间间隔限流 | 定时器 | 网络请求 |
| `std::condition_variable` | 等待信号，跳过中间态 | 锁 | 纯多线程 |

---

## 四、正确性前提

此模式**依赖一个关键假设**：任务队列 `Stq` 是**串行（FIFO）**执行的。
```
提交顺序: task1 → task2 → task3
执行顺序: task1 → task2 → task3  ✓ 串行

若并行执行: task1、task2、task3 同时跑 → 计数器逻辑失效 ✗
```

若队列允许并发，多个任务可能同时通过 `--BcgSync == 0` 的检查，导致重复执行。



------

## 七、适用场景总结

**适合使用此模式的场景：**

- 图像/视频参数实时调整（亮度、对比度、滤镜）
- 搜索框实时过滤（配合串行队列）
- 传感器数据高频采样后的处理

**不适合的场景：**

- 每次调用结果都必须处理（如日志记录、计费）
- 并行任务队列
- 任务之间有依赖关系

------

## 八、一句话总结

> `BcgSync` 利用**串行队列 + 原子计数器**实现了一种轻量级防抖：主线程每次提交任务时将计数 +1，工作线程执行时将计数 -1，若结果仍 > 0 则说明队列中还有更新的任务，直接丢弃当前任务，从而**只让最后一次调用真正生效**，避免无效的中间态计算。